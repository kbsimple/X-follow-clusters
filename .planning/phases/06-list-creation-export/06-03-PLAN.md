---
phase: 06-list-creation-export
plan: '03'
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - "src/list/cli.py"
  - "tests/test_list_creator.py"
  - "tests/test_exporter.py"
  - "tests/conftest.py"
autonomous: true
requirements:
  - "LIST-01"
  - "LIST-02"
  - "LIST-03"
  - "LIST-04"
  - "LIST-05"
  - "EXPORT-01"
  - "EXPORT-02"

must_haves:
  truths:
    - "Phase 6 CLI integrates both list creation and data export in a single execution"
    - "Tests cover core list creation and export functionality"
    - "Export is triggered after successful list creation (or dry-run) without separate invocation"
  artifacts:
    - path: "tests/test_list_creator.py"
      provides: "Unit tests for list creation logic"
    - path: "tests/test_exporter.py"
      provides: "Unit tests for export functions"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
  key_links:
    - from: "tests/test_list_creator.py"
      to: "src/list/creator.py"
      via: "import and test functions"
      pattern: "from src.list import creator"
    - from: "tests/test_exporter.py"
      to: "src/list/exporter.py"
      via: "import and test functions"
      pattern: "from src.list import exporter"
---

<objective>
Integrate export into the Phase 6 CLI (so --execute both creates lists and exports data) and add unit tests for list creation and export functionality.
</objective>

<context>
@src/list/cli.py -- from Plan 06-01, CLI entry point
@src/list/creator.py -- from Plan 06-01, list creation functions
@src/list/exporter.py -- from Plan 06-02, export functions
@src/list/__init__.py -- exports from Plans 06-01 and 06-02
@tests/conftest.py -- shared test fixtures (created in this plan)

# Phase 6 CLI integration
The --execute flow in cli.py should call export_all() after list creation completes successfully.
This means: list creation -> export -> summary report.

# Testing approach
Per 06-RESEARCH.md Validation Architecture:
- tests/test_list_creator.py covers LIST-01 through LIST-05
- tests/test_exporter.py covers EXPORT-01 and EXPORT-02
- conftest.py provides shared fixtures: auth mock, registry mock, temp enrichment cache

<interfaces>
From tweepy Client (mock for testing):
```python
# Mock client behavior:
client.create_list(name=str, description=str, mode=str) -> MockResponse(data={"id": "123", "name": name})
client.add_list_members(list_id=str, user_names=list[str]) -> MockResponse(data={})
client.get_owned_lists(user_id=str, max_results=int) -> MockResponse(data=[])
```

From ApprovalRegistry test fixture:
```python
# Mock registry with one approved cluster:
ApprovalRegistry(
    version=1,
    automation_enabled=False,
    clusters={
        "approved": [{"cluster_id": 1, "cluster_name": "Test Cluster", "size": 10, "silhouette": 0.5, "members": [{"username": "user1"}, ...]}],
        "deferred": [],
        "rejected": []
    }
)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures and config</name>
  <files>tests/conftest.py, pytest.ini</files>
  <read_first>
    - pyproject.toml (dev dependencies already include pytest)
    - src/review/registry.py (ApprovalRegistry structure)
    - src/auth/x_auth.py (XAuth structure)
  </read_first>
  <action>
    Create tests/conftest.py with shared fixtures for Phase 6 tests.

    Fixtures:
    - mock_auth: returns XAuth with test credential strings
    - mock_registry: returns ApprovalRegistry with sample approved and deferred clusters
    - mock_tweepy_client: returns mock client with create_list, add_list_members, get_owned_lists methods
    - temp_enrichment_cache: creates temp dir with sample enrichment JSON files for export tests
    - temp_export_dir: creates temp data/export directory

    Also create pytest.ini or add [tool.pytest] to pyproject.toml:
    - testpaths = tests
    - python_files = test_*.py
    - python_functions = test_*
    - addopts = -x (stop on first failure)

    Verify: pytest --collect-only tests/ shows no collection errors
  </action>
  <verify>
    <automated>pytest --collect-only tests/</automated>
  </verify>
  <done>Test fixtures exist, pytest config valid, no collection errors</done>
  <acceptance_criteria>
    - "tests/conftest.py exists with mock_auth, mock_registry, mock_tweepy_client, temp_enrichment_cache fixtures"
    - "pytest --collect-only tests/ exits 0 with no errors"
    - "grep -n 'def mock_auth' tests/conftest.py returns fixture definition"
    - "grep -n 'def mock_registry' tests/conftest.py returns fixture definition"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Unit tests for list creator</name>
  <files>tests/test_list_creator.py</files>
  <read_first>
    - src/list/creator.py (from Plan 06-01)
    - src/review/registry.py (ApprovalRegistry structure)
    - tests/conftest.py (from Task 1)
  </read_first>
  <action>
    Create tests/test_list_creator.py with unit tests for src/list/creator.py functions.

    Tests to cover:
    1. test_precheck_conflicts_no_conflicts: precheck_conflicts returns empty dict when no names match
    2. test_precheck_conflicts_with_conflicts: precheck_conflicts returns dict with conflicting names
    3. test_create_list_from_cluster_success: create_list_from_cluster calls client.create_list with correct args
    4. test_add_members_chunked: add_members_chunked chunks 250 members into 3 batches of 100, 100, 50
    5. test_list_size_validation_valid: list_size_validation passes for clusters with size 5-50
    6. test_list_size_validation_too_small: list_size_validation skips cluster with size < 5
    7. test_list_size_validation_too_large: list_size_validation skips cluster with size > 50
    8. test_list_size_validation_account_limit: list_size_validation raises when account has >= 1000 lists

    Use pytest.mark.parametrize for size boundary tests.
    Use mock_tweepy_client fixture for all tests.
    Use mock_registry fixture for cluster data.

    Verify: pytest tests/test_list_creator.py -x passes
  </action>
  <verify>
    <automated>pytest tests/test_list_creator.py -x</automated>
  </verify>
  <done>All list creator unit tests pass</done>
  <acceptance_criteria>
    - "pytest tests/test_list_creator.py -x passes with all tests green"
    - "test_precheck_conflicts_no_conflicts exists and passes"
    - "test_precheck_conflicts_with_conflicts exists and passes"
    - "test_create_list_from_cluster_success exists and passes"
    - "test_add_members_chunked exists and verifies 3 batches for 250 members"
    - "test_list_size_validation_valid, test_list_size_validation_too_small, test_list_size_validation_too_large all exist"
    - "test_list_size_validation_account_limit exists and passes"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Unit tests for exporter and CLI integration</name>
  <files>tests/test_exporter.py</files>
  <read_first>
    - src/list/exporter.py (from Plan 06-02)
    - tests/conftest.py (from Task 1)
  </read_first>
  <action>
    Create tests/test_exporter.py with unit tests for src/list/exporter.py functions.

    Tests to cover:
    1. test_export_clusters_to_csv_approved_and_deferred: export_clusters_to_csv creates CSV with both approved and deferred rows
    2. test_export_clusters_to_csv_empty: export_clusters_to_csv creates empty CSV with headers when no clusters
    3. test_export_followers_to_parquet_schema: export_followers_to_parquet creates valid Parquet with correct columns
    4. test_export_followers_to_parquet_skips_special_files: enrichment files named suspended.json, protected.json, errors.json are skipped
    5. test_export_all_runs_both: export_all() calls both functions and returns both paths

    Use temp_enrichment_cache fixture for export tests.
    Use mock_registry fixture for cluster data.

    Also add integration to src/list/cli.py:
    - After successful list creation in --execute mode, call export_all() from the exporter module
    - Print: "[green]Lists created successfully![/green] Now exporting data..."
    - After export: print summary of both list creation and export results
    - This ensures Phase 6 is a single invocation: python -m src.list.cli --execute

    Verify: pytest tests/test_exporter.py -x passes
  </action>
  <verify>
    <automated>pytest tests/test_exporter.py -x</automated>
  </verify>
  <done>All exporter tests pass, CLI integration wired</done>
  <acceptance_criteria>
    - "pytest tests/test_exporter.py -x passes with all tests green"
    - "test_export_clusters_to_csv_approved_and_deferred exists and passes"
    - "test_export_clusters_to_csv_empty exists and passes"
    - "test_export_followers_to_parquet_schema exists and passes"
    - "test_export_followers_to_parquet_skips_special_files exists and passes"
    - "test_export_all_runs_both exists and passes"
    - "src/list/cli.py --execute mode calls export_all() after list creation"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- pytest --collect-only tests/ shows all tests discoverable
- pytest tests/test_list_creator.py -x passes
- pytest tests/test_exporter.py -x passes
- CLI integration: --execute calls both list creation and export
</verification>

<success_criteria>
All Phase 6 requirements (LIST-01 through LIST-05, EXPORT-01, EXPORT-02) have test coverage.
Phase 6 CLI integrates list creation and export in single --execute invocation.
Tests are runnable via pytest tests/ -x and stop on first failure.
</success_criteria>

<output>
After completion, create `.planning/phases/06-list-creation-export/06-03-SUMMARY.md`
</output>