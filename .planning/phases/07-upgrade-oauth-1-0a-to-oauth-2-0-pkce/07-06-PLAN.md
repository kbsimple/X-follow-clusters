---
phase: "07"
plan: "06"
type: upgrade
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - README.md
autonomous: true
requirements:
  - OAUTH2-07
must_haves:
  truths:
    - "README.md documents X_CLIENT_ID, X_CLIENT_SECRET, X_ACCESS_TOKEN, X_REFRESH_TOKEN, X_BEARER_TOKEN env vars"
    - "README.md documents first-run OAuth 2.0 PKCE browser authorization flow"
    - "README.md notes backward compatibility considerations"
  artifacts:
    - path: README.md
      provides: Updated documentation for OAuth 2.0 PKCE setup and first-run flow
  key_links:
    - from: README.md
      to: src/auth/x_auth.py
      via: ensure_authenticated() reference
---

<objective>
Update README.md with OAuth 2.0 PKCE documentation: new environment variables, first-run browser authorization flow, and token persistence.
</objective>

<context>
@README.md

The README currently documents OAuth 1.0a setup. Update it to document OAuth 2.0 PKCE.

New env vars to document:
- X_CLIENT_ID — OAuth 2.0 Client ID (from X Developer Portal)
- X_CLIENT_SECRET — OAuth 2.0 Client Secret
- X_ACCESS_TOKEN — OAuth 2.0 access token (set automatically after first-run auth)
- X_REFRESH_TOKEN — OAuth 2.0 refresh token (set automatically after first-run auth)
- X_BEARER_TOKEN — Optional app-only bearer token for read-only fallback

First-run flow:
1. Set X_CLIENT_ID and X_CLIENT_SECRET in environment
2. Run tool — it will print an authorization URL
3. Open URL in browser and authorize
4. Tokens are saved to data/tokens.json automatically
5. Subsequent runs use stored tokens
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md for OAuth 2.0 PKCE</name>
  <files>README.md</files>
  <action>
    Update the README.md authentication section:

    **Changes needed:**

    1. **Environment Variables section**: Replace OAuth 1.0a vars with OAuth 2.0 PKCE vars:
       - `X_CLIENT_ID` — Required. OAuth 2.0 Client ID from X Developer Portal (App ID)
       - `X_CLIENT_SECRET` — Required. OAuth 2.0 Client Secret
       - `X_ACCESS_TOKEN` — Set automatically after first-run auth. Stored in data/tokens.json.
       - `X_REFRESH_TOKEN` — Set automatically after first-run auth. Stored in data/tokens.json.
       - `X_BEARER_TOKEN` — Optional. App-only bearer token for read-only fallback (not needed for normal use)

    2. **First-run Authorization Flow**: Document the browser-based OAuth 2.0 PKCE flow:
       ```
       1. Export X_CLIENT_ID=your_client_id
       2. Export X_CLIENT_SECRET=your_client_secret
       3. Run the tool (e.g., python -m src.main)
       4. The tool will print an authorization URL
       5. Open the URL in your browser
       6. Authorize the application
       7. The tool will automatically capture the callback and store tokens
       8. Tokens are saved to data/tokens.json for future runs
       ```

    3. **Token Refresh**: Note that refresh tokens are used automatically when access tokens expire. The `offline.access` scope ensures long-lived refresh tokens.

    4. **Backward Compatibility**: Add a note that OAuth 1.0a credentials (X_API_KEY, X_API_SECRET, X_ACCESS_TOKEN_SECRET) are no longer used. Users must obtain new OAuth 2.0 credentials from the X Developer Portal.

    Keep all other README sections unchanged.
  </action>
  <verify>
    <automated>python -c "
import re
content = open('README.md').read()
# Check for OAuth 2.0 references
assert 'X_CLIENT_ID' in content, 'X_CLIENT_ID not documented'
assert 'X_CLIENT_SECRET' in content, 'X_CLIENT_SECRET not documented'
assert 'X_REFRESH_TOKEN' in content, 'X_REFRESH_TOKEN not documented'
assert 'offline.access' in content, 'offline.access scope not documented'
assert 'data/tokens.json' in content, 'token persistence not documented'
print('README.md OAuth 2.0 documentation OK')
"</automated>
  </verify>
  <done>README.md documents OAuth 2.0 PKCE env vars and first-run flow</done>
</task>

</tasks>

<verification>
1. Confirm README.md contains X_CLIENT_ID, X_CLIENT_SECRET, X_REFRESH_TOKEN environment variable documentation
2. Confirm README.md documents first-run browser authorization flow
3. Confirm README.md notes offline.access scope and token refresh
4. Confirm no OAuth 1.0a env var names remain (X_API_KEY, X_API_SECRET, X_ACCESS_TOKEN_SECRET)
</verification>

<success_criteria>
- README.md section on environment variables lists X_CLIENT_ID, X_CLIENT_SECRET, X_ACCESS_TOKEN, X_REFRESH_TOKEN, X_BEARER_TOKEN
- README.md documents first-run flow: set env vars, run tool, open browser URL, authorize, tokens stored automatically
- README.md documents offline.access scope benefit (long-lived refresh tokens)
- README.md notes that OAuth 1.0a credentials are no longer used
- No stale references to X_API_KEY, X_API_SECRET, X_ACCESS_TOKEN_SECRET remain
</success_criteria>

<output>
After completion, create `.planning/phases/07-upgrade-oauth-1-0a-to-oauth-2-0-pkce/07-06-SUMMARY.md`
</output>
