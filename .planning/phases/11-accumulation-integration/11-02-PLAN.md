---
phase: 11-accumulation-integration
plan: 02
type: tdd
wave: 2
depends_on: [11-01]
files_modified: [tests/test_tweet_cache.py]
autonomous: true
requirements: [CACHE-02, CACHE-03]
user_setup: []

must_haves:
  truths:
    - "All integration tests pass after implementation"
    - "Tests validate first fetch, subsequent fetch, deduplication, accumulation"
    - "Test count increases from 35 to 41 (6 new integration tests)"
  artifacts:
    - path: "tests/test_tweet_cache.py"
      provides: "Verified integration tests for accumulation"
      exports: ["TestIntegrationAccumulation"]
  key_links:
    - from: "tests/test_tweet_cache.py"
      to: "src/enrich/api_client.py"
      via: "Mock XEnrichmentClient with TweetCache"
      pattern: "XEnrichmentClient.*tweet_cache"
    - from: "tests/test_tweet_cache.py"
      to: "src/enrich/tweet_cache.py"
      via: "TweetCache fixture and assertions"
      pattern: "temp_tweet_cache"
---

<objective>
Verify all integration tests pass after TweetCache integration implementation.

Purpose: Ensure Phase 11 implementation is complete and validated.
Output: All TestIntegrationAccumulation tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-accumulation-integration/11-CONTEXT.md
@.planning/phases/11-accumulation-integration/11-RESEARCH.md
@.planning/phases/09-tweetcache-core/09-01-SUMMARY.md
@.planning/phases/10-incremental-fetch/10-01-SUMMARY.md
@.planning/phases/10-incremental-fetch/10-02-SUMMARY.md
</context>

<interfaces>
<!-- Tests created in 11-01, verified here -->

From tests/test_tweet_cache.py (created in 11-01):
```python
class TestIntegrationAccumulation:
    """End-to-end integration tests for tweet accumulation."""
    
    def test_first_fetch_persists_tweets(...)
    def test_subsequent_fetch_uses_watermark_and_merges(...)
    def test_deduplication_same_tweet_id_stored_once(...)
    def test_accumulation_across_multiple_runs(...)
    def test_watermark_tracks_newest_tweet(...)
    def test_api_failure_returns_cached_tweets(...)
```
</interfaces>

<feature>
  <name>Integration Test Verification</name>
  <files>tests/test_tweet_cache.py</files>
  <behavior>
    - All 6 integration tests pass
    - Tests validate accumulation end-to-end flow
    - Tests confirm deduplication via PRIMARY KEY
    - Tests verify watermark tracking
    - Tests validate graceful degradation on API failure
  </behavior>
  <implementation>
Run full test suite to verify Phase 11 implementation is complete:

1. Run TestIntegrationAccumulation tests
2. Verify all 6 tests pass
3. Run full test_tweet_cache.py suite to check for regressions
4. Verify test count increased from 35 to 41
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify all integration tests pass</name>
  <files>tests/test_tweet_cache.py</files>
  <action>
Run the integration tests to verify Phase 11 implementation:

```bash
.venv/bin/python -m pytest tests/test_tweet_cache.py::TestIntegrationAccumulation -v
```

Expected: All 6 tests pass (GREEN phase).

If tests fail:
1. Check that test_enrich.py modifications from 11-01 are correct
2. Verify TweetCache integration is properly wired
3. Verify mock setup matches existing patterns from TestIncrementalFetch

The tests were created in 11-01 Task 1. This task verifies they pass after implementation.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tweet_cache.py::TestIntegrationAccumulation -v</automated>
  </verify>
  <done>
All 6 TestIntegrationAccumulation tests pass. Phase 11 validated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run full test suite and verify no regressions</name>
  <files>tests/test_tweet_cache.py</files>
  <action>
Run the full test_tweet_cache.py suite to verify no regressions:

```bash
.venv/bin/python -m pytest tests/test_tweet_cache.py -v
```

Expected: All tests pass (41 total = 35 existing + 6 new).

Verify:
- TestTweetCacheCore tests still pass (17 tests from Phase 9)
- TestTweetCacheWatermark tests still pass (4 tests from Phase 10-01)
- TestIncrementalFetch tests still pass (8 tests from Phase 10-02)
- TestIntegrationAccumulation tests pass (6 new tests)
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tweet_cache.py -v --tb=short</automated>
  </verify>
  <done>
All 41 tests pass. No regressions. Phase 11 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test boundary | Tests mock external dependencies, no real API calls |

## STRIDE Threat Register

No new threats - this plan only verifies existing tests.

</threat_model>

<verification>
- [ ] All 6 TestIntegrationAccumulation tests pass
- [ ] Full test_tweet_cache.py suite passes (41 tests)
- [ ] No regressions in existing tests
</verification>

<success_criteria>
1. All integration tests pass
2. Test count: 41 (35 existing + 6 new)
3. No regressions
</success_criteria>

<output>
After completion, create `.planning/phases/11-accumulation-integration/11-02-SUMMARY.md`
</output>