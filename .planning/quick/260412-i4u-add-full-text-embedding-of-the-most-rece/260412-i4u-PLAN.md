---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enrich/api_client.py
  - src/cluster/embed.py
  - src/enrich/test_enrich.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "API client can fetch 50 recent tweets per account via pagination"
    - "Tweet text is embedded separately as a topical dimension"
    - "tweet_embedding field stored in account cache JSON"
  artifacts:
    - path: "src/enrich/api_client.py"
      provides: "get_recent_tweets() with pagination support for 50 tweets"
      contains: "pagination_token"
    - path: "src/cluster/embed.py"
      provides: "Tweet embedding creation and storage"
      contains: "tweet_embedding"
    - path: "src/enrich/test_enrich.py"
      provides: "Integration testing of 50-tweet fetch + embedding"
  key_links:
    - from: "src/enrich/api_client.py"
      to: "X API v2"
      via: "tweepy Paginator"
      pattern: "get_users_tweets.*pagination_token"
    - from: "src/cluster/embed.py"
      to: "sentence-transformers"
      via: "SentenceTransformer.encode"
      pattern: "tweet_embedding"
---

<objective>
Add full text embedding of the most recent 50 posts for each account as a dedicated topical dimension for clustering.

Purpose: Provide a topical clustering dimension based on what accounts actually post, complementing the existing entity extraction dimension.
Output: Updated API client with pagination support, tweet_embedding field in cache, updated embedding pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Current Implementation

**api_client.py::get_recent_tweets()** (Line 212-237):
- Takes `max_tweets` param (default 5)
- Uses `tweepy.Client.get_users_tweets()` with `max_results=min(max_tweets, 10)` - hardcoded cap at 10
- NO pagination support - can only fetch up to 100 per call, but implementation caps at 10

**embed.py::get_text_for_embedding()** (Line 61-112):
- Already includes `recent_tweets_text` field (appended to combined text)
- Uses `sentence-transformers/all-MiniLM-L6-v2` (384-dim embeddings)
- Creates SINGLE embedding from all combined fields

**Gap**: Need pagination to fetch 50 tweets, and a DEDICATED tweet embedding (separate from combined text embedding).

**X API v2 Pagination**: Uses `pagination_token` from response to fetch next page. Each call returns `next_token` in `meta` field.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pagination support to get_recent_tweets() for fetching 50 tweets</name>
  <files>src/enrich/api_client.py</files>
  <action>
Update `get_recent_tweets()` method to support pagination for fetching up to 50 tweets:

1. Change default `max_tweets` from 5 to 50
2. Remove the hardcoded `min(max_tweets, 10)` cap
3. Implement pagination loop:
   - Use `max_results=100` per call (API max)
   - Loop while tweets collected < max_tweets and `next_token` exists in response
   - Accumulate tweets from each page
4. Handle edge cases:
   - Account with fewer than 50 tweets (return what's available)
   - Rate limiting (log warning, return what's collected)
   - Private/protected accounts (return empty list, already handled)

The X API v2 response structure for get_users_tweets:
```python
response.json() = {
  "data": [{"text": "...", "id": "...", ...}, ...],
  "meta": {"next_token": "...", "result_count": N}
}
```

Pass `pagination_token` param to subsequent calls to get next page.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.enrich.api_client import XEnrichmentClient; import inspect; src = inspect.getsource(XEnrichmentClient.get_recent_tweets); assert 'pagination_token' in src or 'next_token' in src, 'Pagination not implemented'"</automated>
  </verify>
  <done>get_recent_tweets() can fetch 50 tweets via pagination, tested with account having 20+ tweets</done>
</task>

<task type="auto">
  <name>Task 2: Create tweet_embedding field and update embedding pipeline</name>
  <files>src/cluster/embed.py</files>
  <action>
Add a dedicated tweet embedding dimension in embed.py:

1. Add new function `create_tweet_embedding(account: dict) -> np.ndarray | None`:
   - Takes account dict, extracts `recent_tweets_text` field
   - Returns 384-dim embedding using same SentenceTransformer model
   - Returns None if no tweet text available

2. Add new function `store_tweet_embedding(account_id: str, cache_dir: Path) -> None`:
   - Loads account JSON from cache
   - Calls `create_tweet_embedding(account)`
   - Stores result as `tweet_embedding` (list for JSON serialization) in account JSON
   - Writes updated JSON back to cache

3. Update `get_text_for_embedding()` docstring to note that tweet_embedding is a SEPARATE dimension (no code change needed - tweet text already appended via recent_tweets_text field)

The tweet_embedding provides a topical clustering dimension independent of bio/entity extraction.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.cluster.embed import create_tweet_embedding, store_tweet_embedding; print('Functions exist')"</automated>
  </verify>
  <done>tweet_embedding field created and stored in account cache JSON, 384-dim vector from tweet text</done>
</task>

<task type="auto">
  <name>Task 3: Update test_enrich.py to use 50 tweets and create tweet embeddings</name>
  <files>src/enrich/test_enrich.py</files>
  <action>
Update test driver to:
1. Call `get_recent_tweets(account_id, max_tweets=50)` (was default 5)
2. Import and call `store_tweet_embedding(account_id, cache_dir)` after fetching tweets
3. Display tweet_embedding stats in output (dimension, first 5 values preview)

Update the Step 9 section (line 243-273) to:
- Pass `max_tweets=50` to `get_recent_tweets()`
- After caching tweets, call `store_tweet_embedding(account_id, cache_dir)`
- Print tweet_embedding dimension in summary

Add to imports:
```python
from src.cluster.embed import store_tweet_embedding
```
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.enrich.test_enrich import main; import inspect; src = inspect.getsource(main); assert 'max_tweets=50' in src or 'max_tweets = 50' in src, '50 tweets not configured'; assert 'store_tweet_embedding' in src, 'tweet embedding not called'"</automated>
  </verify>
  <done>test_enrich.py fetches 50 tweets and creates tweet_embedding, visible in output</done>
</task>

</tasks>

<verification>
Manual verification after implementation:
1. Run `.venv/bin/python -m src.enrich.test_enrich` with real account
2. Check output shows "50 recent tweets" or "N recent tweets" where N <= 50
3. Check output shows tweet_embedding dimension (384)
4. Verify account JSON contains `tweet_embedding` array field
</verification>

<success_criteria>
- [ ] get_recent_tweets() supports pagination, fetches up to 50 tweets
- [ ] tweet_embedding field stored in account cache (384-dim vector)
- [ ] test_enrich.py updated to use 50 tweets and create embeddings
- [ ] All changes committed
</success_criteria>

<output>
After completion, create `.planning/quick/260412-i4u-add-full-text-embedding-of-the-most-rece/260412-i4u-SUMMARY.md`
</output>