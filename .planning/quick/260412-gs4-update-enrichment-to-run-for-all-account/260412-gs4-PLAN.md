---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/enrich/enrich.py]
autonomous: true
requirements: [QUICK-260412-gs4]
must_haves:
  truths:
    - "User can run enrichment for all accounts with --limit to cap batch size"
    - "Default behavior processes all accounts (no limit)"
  artifacts:
    - path: "src/enrich/enrich.py"
      provides: "enrich_all() with max_accounts parameter and CLI --limit"
  key_links:
    - from: "CLI --limit"
      to: "enrich_all(max_accounts=...)"
      via: "argparse"
---

<objective>
Add configurable account limit to enrichment pipeline.

Purpose: Enable testing with small batches before running full enrichment.
Output: `enrich_all()` with `max_accounts` parameter, CLI `--limit` flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260412-gs4-update-enrichment-to-run-for-all-account/260412-gs4-CONTEXT.md

## Current Behavior

From `src/enrich/enrich.py`:
- `enrich_all()` processes ALL records from following.js
- No limit parameter exists
- Cache files are already overwritten each run (no skip logic)

## Decision Mapping

| Decision | Implementation |
|----------|----------------|
| D-01: Overwrite all | Already current behavior - no changes needed |
| D-02: Change enrich.py | Scope confirmed - modify enrich_all() |
| D-03: Configurable limit | Add `max_accounts` param + `--limit` CLI flag |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add max_accounts parameter to enrich_all()</name>
  <files>src/enrich/enrich.py</files>
  <action>
Per D-03 (configurable limit):

1. Add `max_accounts: int | None = None` parameter to `enrich_all()` function signature

2. After parsing records, truncate the list if limit is provided:
   ```python
   if max_accounts is not None and max_accounts > 0:
       records = records[:max_accounts]
       logger.info("Limiting to %d accounts (of %d total)", max_accounts, total)
       total = len(records)
   ```

3. Add CLI `--limit` argument to argparse:
   ```python
   parser.add_argument(
       "--limit",
       type=int,
       default=None,
       help="Maximum accounts to process (default: all)",
   )
   ```

4. Pass limit to enrich_all() call: `enrich_all(..., max_accounts=args.limit)`

5. Update the docstring to document the new parameter.

Do NOT change cache behavior - the current overwrite-all behavior is correct per D-01.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.enrich import enrich_all; import inspect; sig = inspect.signature(enrich_all); assert 'max_accounts' in sig.parameters"</automated>
  </verify>
  <done>
  - `enrich_all()` accepts `max_accounts` parameter
  - CLI `--limit` flag works: `python -m src.enrich.enrich --help` shows the option
  - With `--limit 5`, only 5 accounts are processed
  </done>
</task>

</tasks>

<verification>
- Unit test: `enrich_all(max_accounts=5)` processes at most 5 records
- CLI test: `python -m src.enrich.enrich --help` shows `--limit` option
- Integration: Run with `--limit 1` against real data (if auth available)
</verification>

<success_criteria>
- `max_accounts` parameter added to `enrich_all()` function
- `--limit` CLI flag added and functional
- Default behavior unchanged (processes all accounts)
</success_criteria>

<output>
After completion, create `.planning/quick/260412-gs4-update-enrichment-to-run-for-all-account/260412-gs4-SUMMARY.md`
</output>