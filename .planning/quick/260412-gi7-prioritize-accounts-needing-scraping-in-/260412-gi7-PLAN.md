---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/enrich/test_enrich.py]
autonomous: true
requirements: [QUICK-260412-gi7]
user_setup: []

must_haves:
  truths:
    - "When scanning cache, accounts with needs_scraping=true are identified"
    - "Accounts needing scraping are prioritized in the sample"
    - "Output shows which accounts need scraping and why (missing bio, missing location, or both)"
  artifacts:
    - path: "src/enrich/test_enrich.py"
      provides: "Enhanced enrichment test driver with scraping prioritization"
  key_links:
    - from: "test_enrich.py cache scanning"
      to: "needs_scraping flag in cached JSON"
      via: "json.loads + needs_scraping field check"
---

<objective>
Update src/enrich/test_enrich.py to prioritize accounts that need scraping.

Purpose: The enrichment API alone doesn't capture bio/location for accounts that lack them. These accounts need the 3scrape pipeline. By prioritizing them in the test sample, we ensure they get attention first.

Output: Enhanced test_enrich.py that identifies and prioritizes scraping-needy accounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/enrich/test_enrich.py
@src/enrich/api_client.py
</context>

<interfaces>
<!-- Key interface from api_client.py lines 186-193 -->
<!-- needs_scraping is set to True when bio or location is missing -->
```python
# From api_client.py _cache_user():
bio = user.get("description") or ""
location = user.get("location") or ""
if not bio or not location:
    enriched["needs_scraping"] = True
else:
    enriched["needs_scraping"] = False
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add scraping-needs identification and prioritization to test_enrich.py</name>
  <files>src/enrich/test_enrich.py</files>
  <action>
    Modify the script to:

    1. **In Step 4 (cache scanning):** In addition to tracking cached_ids, also scan each cached JSON for `needs_scraping: true`. Store these in a `needs_scraping_ids` set.

    2. **In Step 5 (identifying uncached):** Add a new section that identifies cached accounts needing scraping. Print a summary like:
       - "Found X cached accounts needing scraping (missing bio/location)"

    3. **In Step 6 (sample selection):** Change the sample selection logic to prioritize in this order:
       - First: uncached accounts (have no cache file)
       - Second: cached accounts with needs_scraping=true
       - Third (if room remains): other cached accounts (shouldn't happen, but defensive)

    4. **In sample output:** Show which accounts need scraping and why:
       - "NEEDS SCRAPING (missing bio)" or "NEEDS SCRAPING (missing location)" or "NEEDS SCRAPING (missing bio + location)"

    Implementation approach:
    - Add `needs_scraping_ids = set()` alongside `cached_ids`
    - During cache file iteration, parse JSON and check `needs_scraping` flag
    - If true, also check bio and location to determine the reason
    - Store reason in a dict: `needs_scraping_reasons[account_id] = "missing bio" | "missing location" | "missing bio + location"`
    - Update sample selection to take from needs_scraping_ids after uncached_ids
    - Update output to show reason for accounts needing scraping

    Do NOT change the existing cache status loop at lines 147-162 — that shows status AFTER sample selection. Instead, add scraping-needs identification during the cache scanning phase.
  </action>
  <verify>
    <automated>.venv/bin/python -m py_compile src/enrich/test_enrich.py && echo "Syntax OK"</automated>
  </verify>
  <done>
    - Script runs without errors
    - Accounts with needs_scraping=true are identified during cache scan
    - Sample selection prioritizes: uncached -> needs_scraping -> others
    - Output shows which accounts need scraping and why
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No external trust boundaries in this local script modification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | Local test script | accept | No security-relevant changes |
</threat_model>

<verification>
1. Syntax check passes
2. Script executes without errors (manual verification with user's data)
</verification>

<success_criteria>
- Accounts with `needs_scraping: true` are identified and prioritized in the sample
- Output clearly shows which accounts need scraping and the specific reason
- Existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/quick/260412-gi7-prioritize-accounts-needing-scraping-in-/260412-gi7-SUMMARY.md`
</output>